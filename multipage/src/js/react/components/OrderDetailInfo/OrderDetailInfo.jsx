import React, { PureComponent } from 'react'
import PropTypes from 'prop-types';
export default class OrderDetailInfo extends PureComponent {
    static propTypes = {
        /**@prop {import('../../../dataDefine/index.js').OrderInfo} */
        orderInfo:PropTypes.object
    }
    render() {
        
        return (
            <div>
                
            </div>
        )
    }
}
